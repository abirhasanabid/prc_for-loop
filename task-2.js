/**
 * Task: Given an array of strings, use a for loop to print each element in the array.
 */


const name = ['abid', 'nadim', 'shiraj', 'afrin', 'mamun'];

for (let i = 0; i < name.length; i++) {
    console.log(name[i]);
}