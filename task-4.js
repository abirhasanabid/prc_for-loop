/**
 * Task: Use a for loop to calculate the sum of the first 10 natural numbers (1 to 10) and print the result.
 */

let sum = 0;

for (let n = 1; n < 11; n++) {
    sum = sum + n;
}
console.log(sum);